# EZ-EMR Session Context — 2026-05-12 15:51:06

## Session Summary

This session completed the full MVP scaffold for the EZ-EMR platform and added Docker Compose support for running all services together.

---

## What Was Done This Session

### 1. Folder Structure Designed
- Created `plans/markI.md` — canonical folder structure reference document
- Monorepo: `frontend/` (Next.js) + `backend/` (Spring Boot) + `docs/` + `docker-compose.yml`

### 2. Frontend Implemented — Next.js 16.2.6
- Scaffolded with `create-next-app` (TypeScript + Tailwind)
- Feature-based modules: `auth`, `patient`, `appointment`, `subscription`
- Shared UI components: Button, Card, Input, Badge, Table, Modal, AppShell, Sidebar, Topbar, PageContainer
- Pages: `/login`, `/dashboard`, `/patients`, `/appointments`, `/subscription`
- Route guard via `proxy.ts` (Next.js 16 renamed `middleware.ts` → `proxy.ts`)
- API client at `shared/api/client.ts` — points to `http://localhost:8080/api`
- Build status: ✅ Clean

### 3. Backend Implemented — Spring Boot 3.5.0 (Java 17)
- Bootstrapped from Spring Initializr
- 4 modules: `auth`, `patient`, `appointment`, `subscription`
- Each module has: controller / service / repository / entity / dto / mapper layers
- Shared: WebConfig (CORS), SecurityConfig (stateless), GlobalExceptionHandler
- Flyway migration: `V1__init_schema.sql` — patients, appointments, subscriptions
- All entities include `tenant_id UUID NOT NULL` (multi-tenancy rule)
- Walk-in handled via `is_walkin` flag — no separate system
- Build status: ✅ BUILD SUCCESS

### 4. Docker Compose — Full Stack
- `docker-compose.yml` — runs all 3 services: postgres, backend, frontend
- `frontend/Dockerfile` — multi-stage Node.js 23 build
- `backend/Dockerfile` — multi-stage Maven → JRE 17 build
- Dependency chain: `frontend → backend → postgres`

---

## Current File Tree

```
EZ-EMR/
├── frontend/
│   ├── Dockerfile
│   ├── src/
│   │   ├── app/
│   │   │   ├── (auth)/login/page.tsx
│   │   │   ├── (protected)/
│   │   │   │   ├── layout.tsx
│   │   │   │   ├── dashboard/page.tsx
│   │   │   │   ├── patients/page.tsx
│   │   │   │   ├── appointments/page.tsx
│   │   │   │   └── subscription/page.tsx
│   │   │   ├── layout.tsx
│   │   │   ├── page.tsx              ← redirects to /login
│   │   │   └── globals.css
│   │   ├── modules/
│   │   │   ├── auth/components/      ← LoginForm, DashboardView
│   │   │   ├── patient/components/   ← PatientList
│   │   │   ├── appointment/components/ ← AppointmentList
│   │   │   └── subscription/components/ ← SubscriptionView
│   │   ├── shared/
│   │   │   ├── components/           ← Button, Card, Input, Badge, Table, Modal,
│   │   │   │                            AppShell, Sidebar, Topbar, PageContainer
│   │   │   └── api/client.ts         ← fetch wrapper → http://localhost:8080/api
│   │   └── proxy.ts                  ← route guard (Next.js 16 breaking change)
│   ├── .env.local
│   ├── next.config.ts
│   └── package.json
│
├── backend/
│   ├── Dockerfile
│   ├── src/main/java/com/emr/
│   │   ├── EmrApplication.java
│   │   ├── modules/
│   │   │   ├── auth/        ← AuthController, AuthService, LoginRequest/Response DTOs
│   │   │   ├── patient/     ← PatientController, PatientService, PatientRepository,
│   │   │   │                   Patient entity, PatientDto
│   │   │   ├── appointment/ ← AppointmentController, AppointmentService,
│   │   │   │                   AppointmentRepository, Appointment entity,
│   │   │   │                   AppointmentStatus enum, AppointmentDto
│   │   │   └── subscription/ ← SubscriptionController, SubscriptionService,
│   │   │                        SubscriptionRepository, Subscription entity,
│   │   │                        SubscriptionStatus enum, SubscriptionDto
│   │   └── shared/
│   │       ├── config/      ← WebConfig (CORS: localhost:3000)
│   │       ├── security/    ← SecurityConfig (stateless, /auth/** public)
│   │       └── exception/   ← GlobalExceptionHandler, ResourceNotFoundException
│   ├── src/main/resources/
│   │   ├── application.yml
│   │   └── db/migration/V1__init_schema.sql
│   └── pom.xml
│
├── docs/
│   ├── Architecture.md
│   ├── ArchitectureRules.md
│   ├── DevelopmentRules.md
│   ├── FrontendRules.md
│   ├── ThemeRules.md
│   └── Context/
│       ├── 20260512_154207.md   ← first context (post scaffold)
│       └── 20260512_155106.md   ← this file (post Docker setup)
│
├── plans/
│   └── markI.md                 ← folder structure reference
│
├── docker-compose.yml            ← postgres + backend + frontend
└── README.md
```

---

## How to Run

### Option A — Docker Compose (all services)
```bash
cd /Users/tusharpatil/Work/EZ-EMR
docker-compose up --build
```
| Service | URL |
|---|---|
| Frontend | http://localhost:3000 |
| Backend API | http://localhost:8080/api |
| PostgreSQL | localhost:5432 |

### Option B — Local dev (no Docker)
```bash
# Terminal 1 — DB only
docker-compose up postgres -d

# Terminal 2 — Backend
cd backend && ./mvnw spring-boot:run

# Terminal 3 — Frontend
cd frontend && npm run dev
```

---

## Environment Variables

### Backend (`application.yml` / env)
| Variable | Default |
|---|---|
| `DB_HOST` | localhost |
| `DB_PORT` | 5432 |
| `DB_NAME` | emr_db |
| `DB_USER` | emr_user |
| `DB_PASSWORD` | emr_password |
| `SERVER_PORT` | 8080 |

### Frontend
| Variable | Default |
|---|---|
| `NEXT_PUBLIC_API_URL` | http://localhost:8080/api |

---

## Architecture Rules Applied (from docs/)

| Rule | Applied |
|---|---|
| No microservices / Kafka / Redis / CQRS | ✅ Single modular monolith |
| Feature-based frontend (not component-grouped) | ✅ modules/auth, patient, appointment, subscription |
| Single PostgreSQL DB | ✅ Flyway migrations only |
| `tenant_id UUID NOT NULL` on all business tables | ✅ patients, appointments, subscriptions |
| Walk-in = flag, not separate system | ✅ `is_walkin` on Patient + Appointment |
| No cross-module repository access | ✅ only service layer communication |
| REST only | ✅ no GraphQL/RPC |
| TailwindCSS only, one design language | ✅ blue-600 primary, gray-50 bg |
| AppShell (Sidebar + Topbar) on all protected pages | ✅ |

---

## What Is NOT Yet Implemented (Next Steps)

- **JWT auth**: `AuthService.login()` is a stub — throws `UnsupportedOperationException`
- **User entity + repository**: No `users` table or `DB migration` for it yet
- **Patient CRUD**: Only GET list. No create/update/delete endpoints or forms
- **Appointment CRUD**: Only GET list. No scheduling form, no walk-in creation flow
- **Subscription CRUD**: Only GET by tenant. No plan management
- **Module mappers**: `mapper/` packages are empty in all 4 modules
- **Frontend hooks + api**: `modules/*/hooks/` and `modules/*/api/` are empty
- **Frontend types**: `modules/*/types/` are empty
- **Auth token storage**: `proxy.ts` checks `auth_token` cookie but login doesn't set it yet
